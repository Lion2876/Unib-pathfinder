# URL API Laravel — kosongkan jika menggunakan Vite proxy (sudah dikonfigurasi di vite.config.js)
# Uncomment baris di bawah jika tidak menggunakan proxy dan deploy ke server berbeda:
# VITE_API_URL=http://localhost:8000/api
